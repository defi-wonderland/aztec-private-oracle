{
  "name": "private-oracle",
  "version": "0.0.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "compile": "aztec-nargo compile --workspace",
    "codegen:nr": "aztec-cli codegen target/privateoracle-PrivateOracle.json -o src/artifacts --nr && aztec-cli codegen target/token_contract-Token.json -o src/artifacts --nr",
    "codegen:ts": "aztec-cli codegen target/privateoracle-PrivateOracle.json -o src/artifacts --ts && aztec-cli codegen target/token_contract-Token.json -o src/artifacts --ts",
    "codegen:ts:mock": "aztec-cli codegen target/mock_oracle_callback-MockOracleCallback.json -o oracle/src/test/MockCallback/interfaces/ --ts",
    "codegen": "yarn codegen:nr && yarn codegen:ts && yarn codegen:ts:mock",
    "build:contracts": "yarn compile && yarn codegen",
    "test": "yarn build:contracts && NODE_NO_WARNINGS=1 node --experimental-vm-modules $(yarn bin jest) --runInBand --passWithNoTests --testTimeout=30000"
  },
  "jest": {
    "preset": "ts-jest/presets/default-esm",
    "transform": {
      "^.+\\.(ts|tsx)$": [
        "ts-jest",
        {
          "useESM": true
        }
      ]
    },
    "moduleNameMapper": {
      "^(\\.{1,2}/.*)\\.js$": "$1"
    },
    "testRegex": "./src/.*\\.test\\.ts$",
    "rootDir": "./oracle/src"
  },
  "dependencies": {
    "@aztec/aztec.js": "0.20.0",
    "@aztec/foundation": "0.20.0",
    "@aztec/types": "0.20.0",
    "@aztec/accounts": "0.20.0",
    "@types/jest": "^29.5.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "10.9.1",
    "typescript": "5.2.2"
  }
}
